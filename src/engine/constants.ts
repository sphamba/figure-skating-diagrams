export const bladeLength = 0.25;
export const maxBladeLength = 1.0;

// Canvas units per metre, editor drawing only.
export const CANVAS_SCALE = 50;
// Rink board fill and stroke color, reused for pill backgrounds.
export const RINK_COLOR = "#e8e8ec";

// Factor below 1, so a single wheel step moves the strip or zooms the view less.
export const WHEEL_SENSITIVITY = 0.2;

// Matches the app font loaded in main.ts.
export const CANVAS_FONT = '"Inter Variable", sans-serif';
